# الدليل الكامل للنشر على Render.com
## من الصفر إلى التشغيل الكامل

---

# المرحلة 1: رفع الكود على GitHub

## الخطوة 1.1: فتح Shell في Replit
1. في الجانب الأيسر من Replit، اضغط على **Shell**
2. ستظهر نافذة سوداء للأوامر

## الخطوة 1.2: رفع الكود
اكتب هذه الأوامر واحداً تلو الآخر:

```bash
git remote add origin https://github.com/engazalqudra-lab/alsayer-patient-management.git
```
اضغط Enter

```bash
git branch -M main
```
اضغط Enter

```bash
git push -u origin main
```
اضغط Enter

إذا طلب منك اسم المستخدم وكلمة المرور:
- Username: اسم حسابك على GitHub
- Password: استخدم **Personal Access Token** (ليس كلمة المرور العادية)

### كيف تحصل على Personal Access Token:
1. اذهب لـ https://github.com/settings/tokens
2. اضغط **Generate new token (classic)**
3. اختر اسم مثل "Replit Access"
4. حدد صلاحية **repo**
5. اضغط **Generate token**
6. انسخ التوكن واستخدمه بدل كلمة المرور

---

# المرحلة 2: إعداد Google Service Account

## الخطوة 2.1: إنشاء مشروع Google Cloud
1. اذهب إلى: https://console.cloud.google.com
2. في الأعلى، اضغط على اسم المشروع الحالي
3. اضغط **New Project**
4. اكتب اسم: `alsayer-patient-system`
5. اضغط **Create**
6. انتظر حتى يتم الإنشاء
7. اختر المشروع الجديد من القائمة

## الخطوة 2.2: تفعيل Google Sheets API
1. من القائمة الجانبية، اختر **APIs & Services** > **Library**
2. ابحث عن: `Google Sheets API`
3. اضغط عليه ثم اضغط **Enable**
4. ارجع وابحث عن: `Google Drive API`
5. اضغط عليه ثم اضغط **Enable**

## الخطوة 2.3: إنشاء Service Account
1. من القائمة الجانبية، اختر **APIs & Services** > **Credentials**
2. اضغط **Create Credentials** > **Service Account**
3. املأ البيانات:
   - Service account name: `sheets-sync`
   - Service account ID: سيُملأ تلقائياً
4. اضغط **Create and Continue**
5. اضغط **Continue** (تخطي الصلاحيات)
6. اضغط **Done**

## الخطوة 2.4: إنشاء مفتاح JSON
1. في صفحة Credentials، اضغط على Service Account الذي أنشأته
2. اذهب لتاب **Keys**
3. اضغط **Add Key** > **Create new key**
4. اختر **JSON**
5. اضغط **Create**
6. سيتم تحميل ملف JSON - **احفظه في مكان آمن!**

## الخطوة 2.5: نسخ البريد الإلكتروني
1. افتح ملف JSON الذي حملته
2. ابحث عن سطر `"client_email"`
3. انسخ البريد الإلكتروني (مثال: `sheets-sync@alsayer-patient-system.iam.gserviceaccount.com`)

## الخطوة 2.6: مشاركة Google Sheets
1. افتح ملف Google Sheets الخاص بالمرضى (أو أنشئ واحداً جديداً)
2. اضغط **Share** (مشاركة)
3. الصق البريد الإلكتروني من الخطوة السابقة
4. اختر صلاحية **Editor**
5. اضغط **Send**

## الخطوة 2.7: تحويل JSON لسطر واحد
1. افتح ملف JSON في أي محرر نصوص
2. احذف كل الأسطر الجديدة ليصبح سطر واحد
3. أو استخدم هذا الموقع: https://www.textfixer.com/tools/remove-line-breaks.php
4. احفظ النص الناتج - ستحتاجه لاحقاً

---

# المرحلة 3: إنشاء حساب Render.com

## الخطوة 3.1: التسجيل
1. اذهب إلى: https://render.com
2. اضغط **Get Started for Free**
3. اختر **Continue with GitHub**
4. وافق على الصلاحيات

## الخطوة 3.2: ربط GitHub
1. بعد التسجيل، Render سيطلب الوصول لمستودعاتك
2. اضغط **Configure account**
3. اختر **All repositories** أو اختر `alsayer-patient-management` فقط
4. اضغط **Install**

---

# المرحلة 4: إنشاء قاعدة البيانات

## الخطوة 4.1: إنشاء PostgreSQL
1. من Render Dashboard، اضغط **New** > **PostgreSQL**
2. املأ البيانات:
   - Name: `alsayer-db`
   - Database: `alsayer_patients`
   - User: `alsayer`
   - Region: اختر الأقرب لك (مثل Frankfurt أو Singapore)
   - PostgreSQL Version: 15
   - Plan: **Free**
3. اضغط **Create Database**

## الخطوة 4.2: انتظار الإنشاء
1. انتظر 1-2 دقيقة حتى تصبح الحالة **Available**
2. اضغط على قاعدة البيانات لفتح التفاصيل

## الخطوة 4.3: نسخ رابط الاتصال
1. ابحث عن **Internal Database URL**
2. اضغط **Copy** لنسخه
3. احفظه - ستحتاجه في الخطوة القادمة

مثال على الرابط:
```
postgres://alsayer:xxxxxxxxxxxx@dpg-xxxxxxxxx-a.frankfurt-postgres.render.com/alsayer_patients
```

---

# المرحلة 5: إنشاء Web Service

## الخطوة 5.1: إنشاء الخدمة
1. من Render Dashboard، اضغط **New** > **Web Service**
2. اختر **Build and deploy from a Git repository**
3. اضغط **Next**

## الخطوة 5.2: اختيار المستودع
1. ابحث عن `alsayer-patient-management`
2. اضغط **Connect**

## الخطوة 5.3: إعدادات الخدمة
املأ البيانات التالية:

| الحقل | القيمة |
|-------|--------|
| Name | `alsayer-patient-management` |
| Region | نفس منطقة قاعدة البيانات |
| Branch | `main` |
| Runtime | `Node` |
| Build Command | `npm install && npm run build` |
| Start Command | `npm start` |
| Plan | **Free** (أو Starter بـ $7/شهر) |

## الخطوة 5.4: إضافة Environment Variables
اضغط **Advanced** ثم **Add Environment Variable**

أضف المتغيرات التالية:

### 1. DATABASE_URL
- Key: `DATABASE_URL`
- Value: الرابط الذي نسخته من قاعدة البيانات

### 2. TELEGRAM_BOT_TOKEN
- Key: `TELEGRAM_BOT_TOKEN`
- Value: توكن البوت من @BotFather

### 3. TELEGRAM_CHAT_ID
- Key: `TELEGRAM_CHAT_ID`
- Value: معرف الشات

### 4. GOOGLE_SERVICE_ACCOUNT_KEY
- Key: `GOOGLE_SERVICE_ACCOUNT_KEY`
- Value: محتوى ملف JSON (سطر واحد)

### 5. NODE_ENV
- Key: `NODE_ENV`
- Value: `production`

## الخطوة 5.5: النشر
1. اضغط **Create Web Service**
2. انتظر حتى يكتمل البناء (5-10 دقائق)
3. عندما تصبح الحالة **Live**، التطبيق جاهز!

## الخطوة 5.6: الحصول على الرابط
1. في أعلى الصفحة ستجد رابط مثل:
   `https://alsayer-patient-management.onrender.com`
2. هذا رابط تطبيقك!

---

# المرحلة 6: نقل البيانات (اختياري)

إذا كان لديك بيانات مرضى في Replit وتريد نقلها:

## الخطوة 6.1: تصدير من Replit
في Replit Shell:
```bash
pg_dump $DATABASE_URL > backup.sql
```

## الخطوة 6.2: تحميل الملف
1. انقر يمين على `backup.sql`
2. اختر **Download**
3. احذف الملف من Replit:
```bash
rm backup.sql
```

## الخطوة 6.3: استيراد في Render
1. من Render، اذهب لقاعدة البيانات
2. انسخ **External Database URL**
3. استخدم أداة مثل pgAdmin أو DBeaver
4. اتصل بقاعدة البيانات ونفذ محتوى ملف SQL

---

# المرحلة 7: إعداد UptimeRobot

## الخطوة 7.1: إنشاء حساب
1. اذهب إلى: https://uptimerobot.com
2. اضغط **Register for FREE**
3. أدخل بريدك الإلكتروني وكلمة مرور
4. فعّل الحساب من البريد

## الخطوة 7.2: إضافة Monitor
1. من Dashboard اضغط **Add New Monitor**
2. املأ البيانات:

| الحقل | القيمة |
|-------|--------|
| Monitor Type | HTTP(s) |
| Friendly Name | Alsayer Patient App |
| URL | `https://alsayer-patient-management.onrender.com/api/health` |
| Monitoring Interval | 5 minutes |

3. اضغط **Create Monitor**

## الخطوة 7.3: التأكيد
1. انتظر دقيقة
2. تأكد أن الحالة **Up** (أخضر)

---

# المرحلة 8: الاختبار النهائي

## الخطوة 8.1: اختبار التطبيق
1. افتح رابط التطبيق
2. أضف مريض جديد
3. تأكد من الحفظ

## الخطوة 8.2: اختبار Google Sheets
1. افتح ملف Google Sheets
2. تأكد من ظهور بيانات المريض

## الخطوة 8.3: اختبار Telegram
1. افتح الشات على Telegram
2. تأكد من وصول إشعار المريض الجديد

---

# ملخص التكاليف

| الخدمة | الشهري |
|--------|--------|
| Render Web Service | $0 (Free) أو $7 (Starter) |
| Render PostgreSQL | $0 (Free) |
| UptimeRobot | $0 |
| Google Cloud | $0 |
| Telegram | $0 |
| **المجموع** | **$0 - $7/شهر** |

---

# استكشاف الأخطاء

## التطبيق لا يعمل
1. اذهب لـ **Logs** في Render
2. ابحث عن رسائل الخطأ
3. تأكد من صحة `DATABASE_URL`

## Google Sheets لا يعمل
1. تأكد من مشاركة الملف مع Service Account
2. تأكد من صحة `GOOGLE_SERVICE_ACCOUNT_KEY`
3. تأكد من تفعيل APIs

## Telegram لا يعمل
1. تأكد من صحة `TELEGRAM_BOT_TOKEN`
2. تأكد من إضافة البوت للشات
3. تأكد من صحة `TELEGRAM_CHAT_ID`

## التطبيق بطيء
1. الخطة المجانية تنام وتستيقظ
2. UptimeRobot يجب أن يبقيه مستيقظ
3. أو استخدم الخطة المدفوعة

---

# تهانينا! 🎉

تطبيقك الآن يعمل بشكل مستقل تماماً عن Replit!

سيستمر بالعمل حتى لو:
- انتهى اشتراك Replit
- أغلقت Replit
- حذفت المشروع من Replit

البيانات محفوظة في:
- قاعدة بيانات Render
- Google Sheets
- إشعارات Telegram
